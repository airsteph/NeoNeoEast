# NeoNeoEast · 外语学习助手

一个零配置的 Chrome 扩展:在你正常浏览中文网页时,悄悄把**少量**中文词替换成你正在学的外语(英语 **或** 日语),鼠标悬停即可看到释义、音标/词性和例句,并配有一套轻量的间隔复习循环。灵感来自 Toucan,界面采用浅色印刷手册质感。

> 装上就能用,不需要任何账号、不需要联网翻译、不上传任何数据——所有词库、例句与学习记录只存在你自己的浏览器里。

## ✨ 功能特点

- **沉浸式替换**:阅读中文网页时,按设定的间隔把认识的中文词替换成外语,自然地在语境中记单词。
- **中文分词精准切词**:使用浏览器内置的 `Intl.Segmenter` 按真实词边界切分,不会切错词。
- **英 / 日双语,互斥切换**:同一时间只学一种语言,专注不分心。
  - 英语:高考 / 四级 / 六级 / 考研 / 托福 / 雅思 / GRE
  - 日语:JLPT N5 / N4 / N3 / N2 / N1
- **分层词库,层层递进**:按考试等级多选,只替换你选中的层级、且尚未掌握的词。
- **悬停卡片**:释义、音标(英语)/ 词性(日语)、例句(母语例句 + 中文翻译)、以及「无须学习 / 记录生词」快捷按钮。
- **间隔复习**:标记「记录生词」的词会进入复习队列;弹窗内可做每日复习,含每日上限与顺延机制。
- **出词密度可调**:滑块控制"每屏大约出现几个词",按词间隔均匀铺满全文,不扎堆。
- **黑名单**:在指定网站上完全不替换。
- **导出 / 导入**:CSV 导出(全部 / 已掌握 / 在学 / 待复习),以及 `.json` 完整备份,换电脑一键恢复。

## 📦 安装(开发者模式加载)

1. 下载本仓库(点右上角绿色 **Code → Download ZIP**),解压。
2. 打开 Chrome,地址栏输入 `chrome://extensions` 回车。
3. 打开右上角的 **开发者模式**。
4. 点 **加载已解压的扩展程序**,选择解压出来的文件夹。
5. 打开任意一篇长中文文章,刷新页面即可看到效果。点击工具栏的扩展图标可调整语言、层级和密度。

> 需要 Chrome 87+(依赖 `Intl.Segmenter`)。修改设置后,刷新对应网页生效。

## 🗂️ 项目结构

```
NeoNeoEast/
├── manifest.json            # 扩展清单(Manifest V3)
├── background/              # 后台服务 worker:学习状态机、复习调度、统计
├── content-script/          # 注入网页的替换逻辑 + 悬停卡片样式
├── popup/                   # 工具栏弹窗:开关、语言/层级/密度、复习卡片、7 日趋势
├── options/                 # 设置页:黑名单、复习节奏、导出/导入、数据概览
├── data/                    # 分层词库(dict-en.json / dict-ja.json)
└── icons/                   # 图标(16/32/48/128)
```

## 🔒 隐私

本扩展**不联网、不发送任何请求**。词库、例句与中译均随扩展本地打包,所有学习记录保存在浏览器的 `chrome.storage.local` 中。卸载即清除。MIT 开源许可。

部分日语例句来自 Tatoeba 项目(CC-BY 2.0),详见 `data/SOURCES.md`。

## 🛠️ 技术栈

纯原生 JavaScript / HTML / CSS,零构建、零依赖、零联网。Manifest V3 标准,`Intl.Segmenter` 做中文分词,`chrome.storage.local` 存词库与学习数据。

## 📋 更新日志

### v0.6.2（2026-07-08)— 英文词库触发键收敛

- **修复英文反推触发键的一对多噪声**:`dict-en.json` 原先由中文释义分句派生触发键,同一英文词会挂载过多中文触发词,导致网页过度替换(如 `back`/`last`/`country` 各达 14 个触发键)。
- **触发键收敛**:18,169 → 15,052 键(去除 3,117 条噪声);≥4 触发键的英文词 1,223 → 510;单词最大触发键数 14 → 4;平均每词触发键 1.90 → 1.58。
- **严格子集,零丢词**:新触发键集是原集的严格子集(不新增任何触发词),9,547 个英文词零丢失、零新增;英文例句、释义、级别完全未改动。

### v0.6.1（2026-07-08）— 触发词精修与例句形态还原

- **修复单字触发词误替换**（如「占比→占対」）：移除单字 `replace_from`，并精选 44 词白名单恢复必要的单字触发。
- **例句匹配引入日语形态还原**：基于 fugashi 词元还原(活用形归一)，配合四重守卫——词元匹配、读音一致、汉字区分、纯数字拦截。
- **例句覆盖回升至 3,070 / 5,056（60.7%）**,同音异形字(締/閉)零错配。

### v0.5.7（2026-07-07）— 日语释义全面修复

- **meaning_zh 重写**：基于 Japanese-Chinese-thesaurus（公共领域）权威词典，重写了 4,300+ 条日语词条的中文释义，解决了原释义从单句翻译抽取导致的系统性偏差（如「突っ込む→投入」「頼もしい→确实」等错误）。
- **词性规范化**：956 条词条的 `pos` 字段同步更新为标准分类（名词/动词/形容词/副词/接续词等）。
- **模板例句标注修正**：修正 714 条被误标为 `tatoeba` 的模板句式，统一改为 `template`；最终分布：Tatoeba 例句 4,249 条，模板例句 807 条。
- **未覆盖词条**：约 743 条 N2/N1 罕见词及外来词暂未匹配到词典释义，保留原释义，后续版本继续补充。

### v0.5.6（2026-07-07）— 日语词库结构重构

- **词库键名改为日语原词**：`dict-ja.json` 的 key 从「中文替换目标」改为「日语原词」，新增 `replace_from` 字段（数组）存放触发替换的中文词列表。
- **字段重命名**：`phonetic` → `reading`（假名注音）；新增 `example_source` 字段标注例句来源（`tatoeba` / `template`）；移除 `english` 字段。
- **例句来源分类**：93 条模板例句标注为 `template`，4,963 条 Tatoeba 例句标注为 `tatoeba`。
- **释义勘误**：修正 滑る/負ける/済む/折る/光る/散る/貸す 共 7 条 `meaning_zh` 错误。
- **去重合并**：103 条重复条目已合并，词库从 5,164 条精简至 5,056 条。
- **向后兼容**：代码中统一使用 `reading || phonetic`、`english || word` 回退读取，已存储的学习记录无需迁移。
- **受影响文件**：`data/dict-ja.json`、`content-script/content-script.js`、`background/background.js`、`popup/popup.js`、`options/options.js`。

### v0.5.5（2026-07）— 日语例句语料替换

- 用 Tatoeba 语料库（CC-BY 2.0）+ 模板生成替换原有日语例句。
- 在 `data/SOURCES.md` 中添加 Tatoeba 归属声明。

### v0.5.4（2026-07）— 设置页样式修复

- 修复设置页词库覆盖率徽章在浅色背景下文字不可见的问题。

### v0.5.3（2026-07）— 更名与全离线化

- 更名为「外语学习助手」，移除所有联网请求与 Google Fonts 依赖。

## 📄 许可证

[MIT](./LICENSE)
